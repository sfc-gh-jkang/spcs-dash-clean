# Snowflake Configuration for LOCAL DEVELOPMENT ONLY
# These credentials are ONLY used when running locally
# When deployed to Snowpark Container Services, authentication is handled automatically

# Copy this file to .env and replace these with your actual Snowflake credentials
SNOWFLAKE_ACCOUNT=your_account_name.region
SNOWFLAKE_USER=your_username
SNOWFLAKE_PASSWORD=your_password
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=your_database_name
SNOWFLAKE_SCHEMA=PUBLIC

# Development Configuration
DASH_DEBUG=false

# Optional: Additional Snowflake settings
# SNOWFLAKE_ROLE=your_role
# SNOWFLAKE_AUTHENTICATOR=snowflake

# Environment Detection (DO NOT SET THESE - they are auto-detected)
# SNOWFLAKE_CONTAINER_SERVICES=true (auto-set in SPCS)
# SNOWSQL_ACCOUNT=xxx (auto-set in SPCS)

# Instructions:
# 1. Copy this file: cp env.template .env
# 2. Edit .env with your actual values
# 3. Run: docker-compose up --build
# 4. For SPCS deployment, credentials are not needed - authentication is automatic 